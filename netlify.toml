[build]
  command = "npm run build"
  publish = "_site"

[build.environment]
  NODE_VERSION = "20"

# Decap CMS reads/writes content through Netlify's git-gateway, which is
# enabled in the Netlify dashboard (Site settings → Identity), not here.
